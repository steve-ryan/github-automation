```yaml
name: Add Buy Me a Coffee

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  funding:
    runs-on: ubuntu-latest

    steps:
      - name: Test workflow
        run: echo "Workflow syntax is working"
```
